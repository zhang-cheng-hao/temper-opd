diff --git a/on_policy_distillation.sh b/on_policy_distillation.sh
--- a/on_policy_distillation.sh
+++ b/on_policy_distillation.sh
@@ -53,2 +53,2 @@
-export MAX_RESP_LENGTH=7168  # TODO: 31744 /15360 / 7168 / 3072 / 5120
-export MAX_VAL_RESP_LENGTH=7168 # TODO: 15360 / 7168 / 3072
+export MAX_RESP_LENGTH=${MAX_RESP_LENGTH:-7168}  # TODO: 31744 /15360 / 7168 / 3072 / 5120
+export MAX_VAL_RESP_LENGTH=${MAX_VAL_RESP_LENGTH:-7168} # TODO: 15360 / 7168 / 3072
@@ -60 +60 @@
-export N_RESPONSES=4 # TODO: 4 / 8 / 16 / 32 (default: 8)
+export N_RESPONSES=${N_RESPONSES:-4} # TODO: 4 / 8 / 16 / 32 (default: 8)
@@ -79 +79 @@
-export TRAIN_DATASET=datasets/dapo-math-17k.parquet
+export TRAIN_DATASET=${TRAIN_DATASET:-datasets/dapo-math-17k.parquet}
@@ -86 +86 @@
-export TRAIN_DATASET_NAME=DAPO-Math-17k
+export TRAIN_DATASET_NAME=${TRAIN_DATASET_NAME:-DAPO-Math-17k}
@@ -95 +95 @@
-export TEST_DATA_DIR=datasets/test_data
+export TEST_DATA_DIR=${TEST_DATA_DIR:-datasets/test_data}
@@ -97 +97 @@
-TEST_DATASET=${TEST_FILE:-["$TEST_DATA_DIR/AIME25/test.parquet", "$TEST_DATA_DIR/AMC23/test.parquet", "$TEST_DATA_DIR/AIME24/test.parquet"]}
+TEST_DATASET=${TEST_DATASET:-${TEST_FILE:-["$TEST_DATA_DIR/AIME25/test.parquet", "$TEST_DATA_DIR/AMC23/test.parquet", "$TEST_DATA_DIR/AIME24/test.parquet"]}}
@@ -109 +109 @@
-export ACTOR_MODEL_PATH=model/DeepSeek-R1-Distill-Qwen-1.5B
+export ACTOR_MODEL_PATH=${ACTOR_MODEL_PATH:-model/DeepSeek-R1-Distill-Qwen-1.5B}
@@ -130 +130 @@
-export REWARD_MODEL_PATH=model/JustRL-DeepSeek-1.5B
+export REWARD_MODEL_PATH=${REWARD_MODEL_PATH:-model/JustRL-DeepSeek-1.5B}
@@ -133,2 +133,2 @@
-export PROJECT_PATH=checkpoint
-export PARALLEL_SIZE=1
+export PROJECT_PATH=${PROJECT_PATH:-checkpoint}
+export PARALLEL_SIZE=${PARALLEL_SIZE:-1}
@@ -136 +136 @@
-export OUTLINES_CACHE_DIR=~/.cache/outlines/$(uuidgen)
+export OUTLINES_CACHE_DIR=${OUTLINES_CACHE_DIR:-~/.cache/outlines/$(python3 -c 'import uuid; print(uuid.uuid4())')}
@@ -163 +163 @@
-PPO_MAX_TOKEN_LEN_PER_GPU=$(( ((1024 + MAX_RESP_LENGTH) > 32768) ? (1024 + MAX_RESP_LENGTH) : 32768))
+PPO_MAX_TOKEN_LEN_PER_GPU=${PPO_MAX_TOKEN_LEN_PER_GPU:-$(( ((1024 + MAX_RESP_LENGTH) > 32768) ? (1024 + MAX_RESP_LENGTH) : 32768))}
@@ -167 +167 @@
-ray start --head
+ray start --head --include-dashboard=False --disable-usage-stats
@@ -212 +212 @@
-    actor_rollout_ref.rollout.gpu_memory_utilization=0.8 \
+    actor_rollout_ref.rollout.gpu_memory_utilization=${VLLM_GPU_MEMORY_UTILIZATION:-0.8} \
@@ -217 +217 @@
-    actor_rollout_ref.rollout.val_kwargs.n=16 \
+    actor_rollout_ref.rollout.val_kwargs.n=${VAL_N:-16} \
@@ -230 +230 @@
-    reward_model.micro_batch_size_per_gpu=24 \
+    reward_model.micro_batch_size_per_gpu=${REWARD_MICRO_BATCH_SIZE_PER_GPU:-24} \
@@ -239,5 +239,5 @@
-    trainer.n_gpus_per_node=8 \
-    trainer.nnodes=1 \
-    trainer.save_freq=20 \
-    trainer.test_freq=-1 \
-    trainer.total_epochs=1 \
+    trainer.n_gpus_per_node=${N_GPUS_PER_NODE:-8} \
+    trainer.nnodes=${NNODES:-1} \
+    trainer.save_freq=${SAVE_FREQ:-20} \
+    trainer.test_freq=${TEST_FREQ:--1} \
+    trainer.total_epochs=${TOTAL_EPOCHS:-1} \
